<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BibliotecaApp.Views.AgregarLibroPage">
    <StackLayout Padding="20">
        <Entry Placeholder="Título" Text="{Binding Titulo}" />
        <Entry Placeholder="Autor" Text="{Binding Autor}" />
        <Entry Placeholder="ISBN" Text="{Binding ISBN}" />
        <Entry Placeholder="Cantidad de copias" Text="{Binding CantidadCopias}" Keyboard="Numeric" />
        <Button Text="Agregar Libro" Command="{Binding AgregarLibroCommand}" />
    </StackLayout>
</ContentPage>
